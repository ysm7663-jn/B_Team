<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.baraON.dao.MemberDao2">

	<select id="memberSearch" parameterType="String" resultType="int">
		SELECT COUNT(*)
		FROM MEMBER
		WHERE M_ID= #{m_id}
	</select>
	
	<select id="memberNickSearch" parameterType="String" resultType="int">
		SELECT COUNT(*)
		FROM MEMBER
		WHERE M_NICK= #{m_nick}
	</select>

	<insert id="memberInsert" parameterType="com.koreait.baraON.dto.MemberDto2">
		INSERT INTO
		MEMBER (M_NO, M_ID, M_PW, M_PHONE, M_NAME, M_BIRTH, M_NICK, M_EMAIL, M_REGDATE, M_AGREEMENT)
		VALUES (MEMBER_SEQ.NEXTVAL, #{m_id}, #{m_pw}, #{m_phone}, #{m_name}, #{m_birth}, #{m_nick}, #{m_email}, #{m_regDate}, #{m_agreement})
	</insert>
	
	<select id="memberView"  parameterType="int" resultType="com.koreait.baraON.dto.MemberDto2"> 
		SELECT *
		FROM MEMBER
		WHERE M_NO=#{m_no}
	</select>



</mapper>