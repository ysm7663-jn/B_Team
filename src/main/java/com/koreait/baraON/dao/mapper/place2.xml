<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.baraON.dao.PlaceDao2">
	
	<select id="allPlaceSearch"  parameterType="com.koreait.baraON.dto.PlaceDto2" resultType="com.koreait.baraON.dto.PlaceDto2">
		SELECT *
		FROM PLACE
		WHERE S_NO = #{s_no} AND P_DELETE = 0
	</select>
	
	<select id="placeCaSearch"  parameterType="com.koreait.baraON.dto.PlaceDto2" resultType="com.koreait.baraON.dto.PlaceDto2">
		SELECT *
		FROM PLACE
		WHERE S_NO=#{s_no} AND PC_NO=#{pc_no} P_DELETE = 0
	</select>
	
</mapper>